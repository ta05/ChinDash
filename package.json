{
  "name": "chindash",
  "version": "1.0.0",
  "description": "An album sales dashboard that allows the user to view and add album sale information",
  "main": "index.js",
  "dependencies": {},
  "devDependencies": {},
  "scripts": {
    "dev": "concurrently \"cd server && nodemon index.js\" \"cd client && npm run dev\"",
    "dev-client": "concurrently \"cd client && npm run dev\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ta05",
  "license": "ISC"
}
